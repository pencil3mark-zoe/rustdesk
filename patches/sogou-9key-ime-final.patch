diff --git a/flutter/lib/mobile/pages/remote_page.dart b/flutter/lib/mobile/pages/remote_page.dart
--- a/flutter/lib/mobile/pages/remote_page.dart
+++ b/flutter/lib/mobile/pages/remote_page.dart
@@ -20,6 +20,7 @@ import '../../common/widgets/remote_input.dart';
 import '../../models/input_model.dart';
 import '../../models/model.dart';
 import '../../models/platform_model.dart';
+import '../../models/soft_keyboard_commit_utils.dart';
 import '../../utils/image.dart';
 import '../widgets/dialog.dart';
 import '../widgets/custom_scale_widget.dart';
@@ -83,6 +84,7 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
 
   final TextEditingController _textController =
       TextEditingController(text: initText);
+  final CommittedTextMirror _androidCommittedText = CommittedTextMirror('');
 
   _RemotePageState(String id) {
     initSharedStates(id);
@@ -122,6 +124,9 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
           isKeyboardVisible: keyboardVisibilityController.isVisible);
     });
     WidgetsBinding.instance.addObserver(this);
+    if (isAndroid) {
+      _textController.addListener(_handleAndroidTextEditingChange);
+    }
 
     inputModel.keyboardInputAllowed = true;
 
@@ -143,6 +148,9 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
 
   @override
   Future<void> dispose() async {
+    if (isAndroid) {
+      _textController.removeListener(_handleAndroidTextEditingChange);
+    }
     WidgetsBinding.instance.removeObserver(this);
     // Close the session up-front. `gFFI.close()` below only calls `sessionClose`
     // after several awaits (canvas save, image update, the `enable_soft_keyboard`
@@ -324,6 +332,42 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
       inputChar(newStr);
     }
   }
+
+  void _handleAndroidTextEditingChange() {
+    if (!inputModel.keyboardInputAllowed) {
+      return;
+    }
+
+    final value = _textController.value;
+    final edit = _androidCommittedText.update(
+      text: value.text,
+      composing: value.isComposingRangeValid,
+    );
+    if (edit == null) {
+      return;
+    }
+
+    if (isAutoInsertedBracketPair(
+        edit.oldValue, edit.backspaces, edit.insert)) {
+      bind.sessionInputString(sessionId: sessionId, value: edit.insert);
+      _openKeyboardUnlocked();
+      return;
+    }
+
+    for (var i = 0; i < edit.backspaces; ++i) {
+      inputModel.inputKey('VK_BACK');
+    }
+
+    if (edit.insert.isEmpty) {
+      return;
+    }
+
+    // A single non-ASCII character must use the text/sequence path too.
+    // On Linux/X11, the legacy KeyEvent.chr path becomes Enigo
+    // key_down/key_up; its libxdo fallback treats CJK as a key name and can
+    // silently ignore it. KeyEvent.seq uses xdo_enter_text_window instead.
+    if (shouldSendAsTextSequence(edit.insert)) {
+      bind.sessionInputString(sessionId: sessionId, value: edit.insert);
+    } else {
+      inputChar(edit.insert);
+    }
+  }
 
   void _handleNonIOSSoftKeyboardInput(String newValue) {
     var oldValue = _value;
@@ -370,6 +414,11 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
     if (!inputModel.keyboardInputAllowed) {
       return;
     }
+    // Android is driven by the TextEditingController listener so composing
+    // range changes are visible even when the text itself does not change.
+    if (isAndroid) {
+      return;
+    }
     if (isIOS) {
       _handleIOSSoftKeyboardInput(newValue);
     } else {
@@ -417,6 +466,7 @@ class _RemotePageState extends State<RemotePage> with WidgetsBindingObserver {
     gFFI.invokeMethod("enable_soft_keyboard", true);
     // destroy first, so that our _value trick can work
     _value = initText;
+    _androidCommittedText.reset(initText);
     _textController.text = _value;
     setState(() => _showEdit = false);
     _timer?.cancel();
